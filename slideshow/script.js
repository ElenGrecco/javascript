/**
 * Slide show
 * @author Elen Grecco
 */

// array(vetor) de imagens
let slides = ["banner1.jpg", "banner2.jpg", "banner3.jpg"]
let intervalo = 3000 // (3000ms (1se por imagem))
let indice = 0 // esta variavel é igual apoio a lógica (índice do array)

// Função para a troca das imagens
function show() {
    // capturar o id do banner
    const slide = document.getElementById('slide')

}

// executar a função ao iniciar o documento js
show()

